import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.choipeanut.salecalendar',
  appName: 'Sale Calendar',
  webDir: 'apk-web'
};

export default config;
